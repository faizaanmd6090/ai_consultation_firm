"""
Shared shapes used across the project (Phase 2).

This package holds small, importable helpers so the rest of the codebase stays
modular. See agent_output.py for the consulting agent return format.
"""
